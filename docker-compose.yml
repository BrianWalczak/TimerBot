services:
  app:
    build: .
    image: timerbot
    container_name: timerbot
    ports:
      - "8080:8080"
    volumes:
      - ./data:/usr/src/app/data
    environment:
      CONFIG_FILE: /usr/src/app/data/config.json
      DATABASE_URL: file:/usr/src/app/data/data.db
    restart: unless-stopped
